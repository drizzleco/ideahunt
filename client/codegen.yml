overwrite: true
schema: "../schema.graphql"
documents:
  - "**/*.tsx"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      operationResultSuffix: "Model"
